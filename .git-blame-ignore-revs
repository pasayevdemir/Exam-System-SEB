# Commits listed here are skipped by `git blame`, so a mechanical reformat does
# not become the answer to "who last touched this line".
#
# GitHub's blame view reads this file automatically. Locally, opt in once:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add commits that change formatting and nothing else.

# Format the whole tree with Pint
6498ae2101460b153f49d6db8732afbfd2753b5d
